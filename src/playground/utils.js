/* eslint-disable */
console.log("util.js is running");

export const square = x => x * x;

export const add = (a, b) => a + b;

export default (a, b) => a - b; // can be only after a variable or inline like this one

// export { square, add, substract as default };

// exports - single default - lots of named exports
